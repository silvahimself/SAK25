//using Murr;
using SAK25.Helpers;

namespace SAK25_console
{
    internal class Program
    {
        static async Task Main(string[] args)
        {
            Console.WriteLine("File paths (comma separated):");
            var files = Console.ReadLine();
            //var m = new Murmur();
            //await m.CreateMermaid(files.Split(','));
        }
    }
}
